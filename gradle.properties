# Gradle configuration for reconciliation service

# Use Gradle daemon for faster builds
org.gradle.daemon=true

# Increase memory for better performance
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# Enable parallel execution
org.gradle.parallel=true

# Enable build cache
org.gradle.caching=true

# Configure Java toolchain (automatically finds Java 17)
org.gradle.java.home=/Users/andywiranata/Library/Java/JavaVirtualMachines/corretto-17.0.12/Contents/Home
